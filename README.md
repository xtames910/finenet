# finenet
The code file performs stock price analysis and forecasting using historical data (Open, High, Low, Close). It includes data preprocessing, normalization, exploratory data analysis (EDA), and applies models such as ARIMA and LSTM/GRU to predict trends and detect abnormal fluctuations.
